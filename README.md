# canister-worm-scanner
## How to use it

Create the script: Save the code above as scan.js in the root of the project you want to check.

Run it: Open your terminal and run the following in the root of the repo you want to check:

```Bash
node scan.js
```

The Result: It will combine dependencies and devDependencies and print a table of any matches found.
